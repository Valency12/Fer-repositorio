#include <iostream>

using namespace std;

int main()
{

  // Un array puede tener varios tipos de datos
  int arrayInt[5] = {0, 1, 2, 3, 4};
  float arrayFloat[3] = {0.1, 0.02, 0.33};
  string arrayString[2] = {"Diego", "Fernando"};

  // Pero todos los valores tienen que coincidir con el mismo tipo de dato

  return 0;
}